import 'package:flutter/material.dart';

class DCalendarScreen extends StatelessWidget {
  const DCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('진료 캘린더 - 구현 예정'),
    );
  }
}
